#include<stdio.h>
int main()
{
    double a, b, sum;

    printf("Enter two integer numbers: ");
    scanf("%d %d", &a, &b);

    sum = a + b;

    printf("Sum is %d\n", sum);

    return 0;
}
